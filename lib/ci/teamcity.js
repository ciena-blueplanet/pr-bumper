'use strict'

require('../typedefs')

const CiBase = require('./base')

/**
 * CI interface for TeamCity
 *
 * @class
 * @implements {Ci}
 */
class TeamCity extends CiBase {
  // nothing to override
}

module.exports = TeamCity
